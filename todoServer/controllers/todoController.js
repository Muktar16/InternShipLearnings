import mongoose from "mongoose";
// import Todo from './'
//const Todo = mongoose.model('Todo');

export const createNewTodo = (req,res)=>{
    // var newTodo = new Todo();
    // newTodo.name = req.body.name;
    // newTodo.priority = req.body.priority;
    // newTodo.todoID = req.body.todoID;
    // newTodo.time = req.body.time;


    // newTodo.save((err, doc) => {
    //     if (!err)
    //         res.send(doc);
    //     else {
    //         if (err.code == 11000)
    //             res.status(422).send(['Todo already saved!!!!']);
    //         else
    //             return next(err);
    //     }
    // });
}